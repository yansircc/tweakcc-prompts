{
  "name": "tweakcc-prompts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "apply": "node /Users/yansir/code/tmp/tweakcc/dist/index.mjs --apply",
    "restore": "cd /Users/yansir/.claude/local && rm -rf node_modules package-lock.json && npm install",
    "validate": "npx tweakcc --apply && ccc -p \"Reply me YAHA for testing purpose\"",
    "check": "bun run scripts/check.ts",
    "check:json": "bun run scripts/check.ts --json"
  },
  "devDependencies": {
    "bun-types": "^1.2.17",
    "husky": "^9.1.7"
  }
}